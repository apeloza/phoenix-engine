import { connect } from 'react-redux';
import TextBox from '../components/TextBox';

const mapStateToProps = (state, ownProps) => {
  return{

  }
}

const WiredTextBox = connect(mapStateToProps)(TextBox)

export default WiredTextBox;
